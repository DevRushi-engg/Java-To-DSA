Strings in Java
===============

String in Java is a data type that is used to store a sequence of characters.
It can be declared using the keyword "String".
Strings are immutable, which means that once a string is created, it cannot be changed.
Strings can be created using string literals, which are enclosed in double quotes.

